<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="hr.nas2skupa.eleventhhour">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.auth.SignInActivity_"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.SignInTheme"
            android:windowSoftInputMode="stateHidden|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="hr.nas2skupa.eleventhhour.SIGN_OUT"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.MainActivity_"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/MaterialTheme.Light.DarkToolbar.TranslucentStatus">
            <intent-filter>
                <action android:name="ch.photrack.discharge.ACTION_HOME"/>
                <action android:name="ch.photrack.discharge.ACTION_CALENDAR"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.CategoryActivity_"
            android:screenOrientation="portrait"
            android:theme="@style/MaterialTheme.Light.DarkToolbar.TranslucentStatus"/>
        <activity
            android:name=".ui.ProviderActivity_"
            android:screenOrientation="portrait"
            android:theme="@style/MaterialTheme.Light.TranslucentStatus"/>
        <activity
            android:name=".ui.MapActivity"
            android:label="@string/title_activity_map"
            android:screenOrientation="portrait"
            android:theme="@style/MaterialTheme.Light.TranslucentStatus"/>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key"/>
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="cd3d2c728913a7a16aea78e2d76ccc5fa27f9892"/>
    </application>

</manifest>